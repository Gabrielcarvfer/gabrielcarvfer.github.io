<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.2.1">Jekyll</generator><link href="/feed.xml" rel="self" type="application/atom+xml" /><link href="/" rel="alternate" type="text/html" /><updated>2021-11-30T01:47:10+00:00</updated><id>/feed.xml</id><title type="html">blank</title><subtitle>A bunch of random stuff
</subtitle><entry><title type="html">ns-3 transition from waf to CMake</title><link href="/2021/11/29/ns3-waf-to-cmake-transition.html" rel="alternate" type="text/html" title="ns-3 transition from waf to CMake" /><published>2021-11-29T00:00:00+00:00</published><updated>2021-11-29T00:00:00+00:00</updated><id>/2021/11/29/ns3-waf-to-cmake-transition</id><content type="html" xml:base="/2021/11/29/ns3-waf-to-cmake-transition.html">&lt;h1 class=&quot;unnumbered&quot; data-number=&quot;&quot; id=&quot;introduction&quot;&gt;Introduction&lt;/h1&gt;
&lt;p&gt;The ns-3 network simulator is one of the most used network simulators in the academia, however, it has a very steep learning curve, especially when you are still learning C++ and the standards themselves.&lt;/p&gt;
&lt;p&gt;Not their fault in any way, it is just that modeling complex systems is hard by itself.&lt;/p&gt;
&lt;p&gt;Ever-evolving standards, adding more and more features as the time goes while keeping backwards compatibility also doesn’t help.&lt;/p&gt;
&lt;p&gt;What can we do to help?&lt;/p&gt;
&lt;p&gt;Better guides? Maybe, but I do not think they help as much. If it was something interactive, it could be pretty cool. I have tried setting up Jupyter notebooks for more interactive guides using the power of &lt;a href=&quot;https://github.com/jupyter-xeus/xeus-cling&quot;&gt;Cling-Xeus&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;Better examples? Maybe. Some examples are very simple while we have much more complicated scenarios in published papers.&lt;/p&gt;
&lt;p&gt;Better docs? I personally don’t think they help as much when you’re already lost.&lt;/p&gt;
&lt;p&gt;Maybe include a parser to be able to describe the simulations from a higher level description language like other simulators do? That does not sound like a bad idea, since setting up some things can be quite complicated if you are just getting started.&lt;/p&gt;
&lt;p&gt;Better tooling? This is where I come in.&lt;/p&gt;
&lt;h1 class=&quot;unnumbered&quot; data-number=&quot;&quot; id=&quot;build-systems&quot;&gt;Build systems&lt;/h1&gt;
&lt;p&gt;Something so simple as a build system should not be such a problem, am I right? Well, it is not if you already know what you are doing, how to get things done, etc.&lt;/p&gt;
&lt;p&gt;If you are part of the group just getting started into everything at once, it is overwhelming and your productivity is basically none for a while, which can be frustrating. I was one of these.&lt;/p&gt;
&lt;p&gt;So, how bad can it really be? Well, pretty bad. Since I have been used to IDEs, where projects can be organized neatly, settings are managed graphically, auto-completion works, coding mistakes are properly highlighted, you can easily navigate and refactor the code and my preferred feature: VISUAL DEBUGGING.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/assets/img/2021-11-29-ns3-waf-to-cmake-transition/visual_debugging.png&quot; style=&quot;width:100.0%&quot; alt=&quot;image&quot; /&gt;&lt;/p&gt;
&lt;p&gt;Yes, I like visual debuggers. Really like them. For a few reasons, being the following two the main ones:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;p&gt;I can see the values of the variables not only in the memory watches panels, but inlined into the code&lt;/p&gt;&lt;/li&gt;
&lt;li&gt;&lt;p&gt;I can see the callstack&lt;/p&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;But you may ask me: &lt;em&gt;-What the devil build systems got to do with visual debugging?&lt;/em&gt;.&lt;/p&gt;
&lt;p&gt;And that is a pretty good question. While build systems do not prevent you from using visual debuggers, it limits the amount of information and insights you can get from the code. Some IDEs, for example, do not even allow for that.&lt;/p&gt;
&lt;p&gt;Why all of this matters? &lt;a href=&quot;https://waf.io/&quot;&gt;&lt;strong&gt;WAF&lt;/strong&gt;&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;Waf is a pretty cool build system written entirely in Python and is supper easy to distribute with your project, so it needs only Python installed (which you would probably already have anyways) and whatever your project needs to get build (e.g. compiler).&lt;/p&gt;
&lt;p&gt;ns-3 have been using Waf for a while, and it has been working well for quite a long time, which goes to show it is not a bad build system in any way.&lt;/p&gt;
&lt;p&gt;My grudges with Waf is just that it does not support IDEs and the organization in different steps, plus how things are organized seem overly complicated for me. What I dislike the most however is that it does not produce intermediary build system files that I can inspect visually and discover what I am doing wrong.&lt;/p&gt;
&lt;p&gt;So I started working on CMake support...&lt;/p&gt;
&lt;h1 class=&quot;unnumbered&quot; data-number=&quot;&quot; id=&quot;ns-3-with-cmake&quot;&gt;ns-3 with CMake&lt;/h1&gt;
&lt;p&gt;Since I started my masters, my advisor said I should start working with ns-3. If it was not for that, I would probably be using ns-2 or OMNET++.&lt;/p&gt;
&lt;p&gt;Anyways, so I started rewriting everything ns-3 needed on CMake. Took quite some time to write everything, since I was not only translating Waf, but &quot;reverse engineering&quot;. I admit this was pretty stupid, but I simply could not understand how Waf did its magic (if it had written makefiles or something like that, it would have been easier and faster).&lt;/p&gt;
&lt;p&gt;Since 2018 I have been gathering feedback and refactoring everything to &lt;a href=&quot;https://gitlab.com/nsnam/ns-3-dev/-/merge_requests/460&quot;&gt;upstream that support&lt;/a&gt;, which seems like it is going to happen soon (release 3.36).&lt;/p&gt;
&lt;p&gt;With CMake, all major IDEs are supported from the get go: Microsoft &lt;a href=&quot;https://gabrielcarvfer.github.io/NS3/installation/visualstudio&quot;&gt;Visual Studio&lt;/a&gt; and &lt;a href=&quot;https://gabrielcarvfer.github.io/NS3/installation/visualcode&quot;&gt;Visual Code&lt;/a&gt;, &lt;a href=&quot;https://gabrielcarvfer.github.io/NS3/installation/clion&quot;&gt;Jetbrains CLion&lt;/a&gt;, &lt;a href=&quot;https://gabrielcarvfer.github.io/NS3/installation/xcode&quot;&gt;Apple XCode&lt;/a&gt;, &lt;a href=&quot;https://gabrielcarvfer.github.io/NS3/installation/codeblocks&quot;&gt;Code::Blocks&lt;/a&gt;, &lt;a href=&quot;https://gabrielcarvfer.github.io/NS3/installation/eclipse&quot;&gt;Eclipse&lt;/a&gt;, CodeLite and others.&lt;/p&gt;
&lt;p&gt;Of course CMake can also be called directly via &lt;a href=&quot;https://gabrielcarvfer.github.io/NS3/installation/terminal&quot;&gt;the command-line&lt;/a&gt;, but commands are very verbose.&lt;/p&gt;
&lt;p&gt;Command-line users will be happy to know they were not forgotten with the &lt;a href=&quot;https://gabrielcarvfer.github.io/NS3/installation/ns3waf&quot;&gt;ns3&lt;/a&gt; wrapper script for CMake (previously known as fakewaf and ns3waf). The script provides pretty much the same functionality expected from Waf, including exporting library paths, making programs just work instead of having to export the path first.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;My suggestion&lt;/strong&gt;: use the ns3 wrapper when you need it. For example, when using IDEs that do not natively support CMake projects such as Xcode, Code::Blocks and Eclipse, you will need to specify the proper CMake Generator to get a project for them. Also, you will need to close the IDE, refresh the CMake cache manually (re-run ’ns3 configure’) whenever you add/remove a source file, create a new module or add a new dependency (either module or library), then you can open the IDE once again and continue using. I know it is pretty bad and that is why I suggest either CLion (my preferred IDE) or VsCode, which works surprisingly well with CMake.&lt;/p&gt;
&lt;h2 class=&quot;unnumbered&quot; data-number=&quot;&quot; id=&quot;what-changes-for-a-module-developer&quot;&gt;What changes for a module developer?&lt;/h2&gt;
&lt;p&gt;Not that much. I have purposefully made it very similar to make the transition as smooth as possible. Copy and paste source lists to the appropriate variables, then clean semicolons and quotes and you are good to go if your module does not have any special needs (e.g. external library or options).&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/assets/img/2021-11-29-ns3-waf-to-cmake-transition/modules_transition.png&quot; style=&quot;width:100.0%&quot; alt=&quot;image&quot; /&gt;&lt;/p&gt;
&lt;p&gt;If you need user-provided paths to look for libraries, you can follow example from the brite:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;    # This part is similar the conf part in a wscript
    # I know it is pretty confusing, but this is due to CMake limitations
    #
    # First we declare/set a variable that points to a PATH in the CMake CACHE
    #    (a.k.a. this value won&amp;#39;t be lost accross runs)
    # Then we declare/set a variable also stored in the cache,
    #    but now it is used INTERNALly by us to determine if BRITE was
    #    found in the NS3_WITH_BRITE path
    set(NS3_WITH_BRITE &amp;quot;&amp;quot; CACHE PATH &amp;quot;Build with brite support&amp;quot;)
    set(NS3_BRITE &amp;quot;OFF&amp;quot; CACHE INTERNAL &amp;quot;ON if Brite is found in NS3_WITH_BRITE&amp;quot;)

    # If a path for Brite was not given, we just skip the entire module
    #     and pretend it does not exist
    if(NOT NS3_WITH_BRITE)
      return()
    endif()

    # If a path for Brite was given, we search for both the library and
    #     the header in the NS3_WITH_BRITE folder
    find_library(brite_dep brite
                 PATHS ${NS3_WITH_BRITE}
                 PATH_SUFFIXES /build /build/lib /lib
                 )
    find_file(brite_header Brite.h
              HINTS ${NS3_WITH_BRITE}
              PATH_SUFFIXES /build /build/include /include
              )

    # If both are not found, we return a message indicating it is the
    #     case and stop processing this module by returning to the src folder
    if(NOT (brite_dep AND brite_header))
      message(STATUS &amp;quot;Brite was not found in ${NS3_WITH_BRITE}&amp;quot;)
      return()
    endif()

    # If both were found, we get the directory containing
    #     the brite header and use it as an include folder
    get_filename_component(brite_include_folder ${brite_header} DIRECTORY)
    include_directories(${brite_include_folder})

    # We also set the NS3_BRITE variable, indicating it was found
    set(NS3_BRITE &amp;quot;ON&amp;quot; CACHE INTERNAL &amp;quot;ON if Brite is found in NS3_WITH_BRITE&amp;quot;)

    # your module name (in this case brite, which can be used by other libraries
    #    including ${libbrite} in their libraries_to_link variable)
    set(name brite)

    # your source files
    set(source_files helper/brite-topology-helper.cc)

    # your header files
    set(header_files helper/brite-topology-helper.h)

    # link to dependencies
    set(libraries_to_link
            ${libnetwork} # notice the ns-3 modules required by libbrite
            ${libcore}
            ${libinternet}
            ${libpoint-to-point}
            ${brite_dep} # notice the brite_dep library
            )

    # your test source files
    set(test_sources test/brite-test-topology.cc)

    # The magic macro that does Waf-like magic
    build_lib(&amp;quot;${name}&amp;quot;
              &amp;quot;${source_files}&amp;quot;
              &amp;quot;${header_files}&amp;quot;
              &amp;quot;${libraries_to_link}&amp;quot;
              &amp;quot;${test_sources}&amp;quot;
              )&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;If you need to find a library with a FindPackage(), the config-store module serves as an example:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;
    # Search for HarfBuzz and GTK3
    #     (we currently do this in ns-3-dev/buildsupport/macros_and_definitions.cmake)

    # Only check for GTK3 if the user set this flag
    #     (currently in ns-3-dev/CMakeLists.txt)
    if(${NS3_GTK3})
      find_package(HarfBuzz QUIET)
      if(NOT ${HarfBuzz_FOUND})
        message(STATUS &amp;quot;Harfbuzz is required by GTK3 and was not found.&amp;quot;)
      else()
        set(CMAKE_SUPPRESS_DEVELOPER_WARNINGS 1 CACHE BOOL &amp;quot;&amp;quot;)
        find_package(GTK3 QUIET)
        unset(CMAKE_SUPPRESS_DEVELOPER_WARNINGS CACHE)
        if(NOT ${GTK3_FOUND})
          message(STATUS &amp;quot;GTK3 was not found. Continuing without it.&amp;quot;)
        else()
          message(STATUS &amp;quot;GTK3 was found.&amp;quot;)
        endif()
      endif()
    endif()

    # Search for LibXml2
    #     we currently do this in buildsupport/macros_and_definitions.cmake)
    find_package(LibXml2 QUIET)
    if(NOT ${LIBXML2_FOUND})
      message(STATUS &amp;quot;LibXml2 was not found. Continuing without it.&amp;quot;)
    else()
      message(STATUS &amp;quot;LibXml2 was found.&amp;quot;)
      add_definitions(-DHAVE_LIBXML2)
    endif()

    set(name config-store)

    # add optional sources if an optional GTK3 dependency is found
    if(${GTK3_FOUND})
      set(gtk3_sources model/display-functions.cc
                       model/gtk-config-store.cc
                       model/model-node-creator.cc
                       model/model-typeid-creator.cc
      )

      set(gtk3_headers model/gtk-config-store.h)
      include_directories(${GTK3_INCLUDE_DIRS} ${HarfBuzz_INCLUDE_DIRS})
      set(gtk_libraries ${GTK3_LIBRARIES})
    endif()

    # add optional sources if an optional LibXml2 dependency is found
    if(${LIBXML2_FOUND})
      set(xml2_sources model/xml-config.cc)
      set(xml2_libraries ${LIBXML2_LIBRARIES})
      include_directories(${LIBXML2_INCLUDE_DIR})
    endif()

    # add optional sources to the source_files list
    set(source_files
        ${gtk3_sources}
        ${xml2_sources}
        model/attribute-default-iterator.cc
        model/attribute-iterator.cc
        model/config-store.cc
        model/file-config.cc
        model/raw-text-config.cc
    )

    # add optional header to the header_files list
    set(header_files ${gtk3_headers} model/file-config.h model/config-store.h)

    # add optional libraries to the libraries_to_link list
    set(libraries_to_link ${libcore} ${libnetwork} ${xml2_libraries} ${gtk_libraries})

    set(test_sources)

    build_lib(&amp;quot;${name}&amp;quot;
              &amp;quot;${source_files}&amp;quot;
              &amp;quot;${header_files}&amp;quot;
              &amp;quot;${libraries_to_link}&amp;quot;
              &amp;quot;${test_sources}&amp;quot;
              )
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;You may ask: &lt;em&gt;-Is there yet another way of finding libraries?&lt;/em&gt;. And the answer could not be other than: of course there is.&lt;/p&gt;
&lt;p&gt;For libraries that use pkg-config, you can follow the fd-net-device example.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;    # Include CMake script to use pkg-config
    include(FindPkgConfig)
    # If pkg-config was found, search for dpdk
    if(PKG_CONFIG_FOUND)
      pkg_check_modules(DPDK libdpdk)
    endif()
    ...
    # Reset cached variable
    set(ENABLE_DPDKDEVNET False CACHE INTERNAL &amp;quot;&amp;quot;)
    ...
    # Set cached variable if both pkg-config and libdpdk are found
    if(PKG_CONFIG_FOUND AND DPDK_FOUND)
      set(ENABLE_DPDKDEVNET True CACHE INTERNAL &amp;quot;&amp;quot;)
    endif()&lt;/code&gt;&lt;/pre&gt;
&lt;h1 class=&quot;unnumbered&quot; data-number=&quot;&quot; id=&quot;the-end&quot;&gt;The end&lt;/h1&gt;
&lt;p&gt;Feel free to provide feedback either on the &lt;a href=&quot;https://groups.google.com/g/ns-3-users&quot;&gt;ns-3-users group&lt;/a&gt;, via the &lt;a href=&quot;https://gitlab.com/nsnam/ns-3-dev/-/merge_requests/460&quot;&gt;open MR&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;You can also find me on the &lt;a href=&quot;https://ns-3.zulipchat.com/&quot;&gt;ns-3 Zulip&lt;/a&gt;.&lt;/p&gt;</content><author><name>Gabriel</name></author><summary type="html">Introduction The ns-3 network simulator is one of the most used network simulators in the academia, however, it has a very steep learning curve, especially when you are still learning C++ and the standards themselves. Not their fault in any way, it is just that modeling complex systems is hard by itself. Ever-evolving standards, adding more and more features as the time goes while keeping backwards compatibility also doesn’t help. What can we do to help? Better guides? Maybe, but I do not think they help as much. If it was something interactive, it could be pretty cool. I have tried setting up Jupyter notebooks for more interactive guides using the power of Cling-Xeus. Better examples? Maybe. Some examples are very simple while we have much more complicated scenarios in published papers. Better docs? I personally don’t think they help as much when you’re already lost. Maybe include a parser to be able to describe the simulations from a higher level description language like other simulators do? That does not sound like a bad idea, since setting up some things can be quite complicated if you are just getting started. Better tooling? This is where I come in. Build systems Something so simple as a build system should not be such a problem, am I right? Well, it is not if you already know what you are doing, how to get things done, etc. If you are part of the group just getting started into everything at once, it is overwhelming and your productivity is basically none for a while, which can be frustrating. I was one of these. So, how bad can it really be? Well, pretty bad. Since I have been used to IDEs, where projects can be organized neatly, settings are managed graphically, auto-completion works, coding mistakes are properly highlighted, you can easily navigate and refactor the code and my preferred feature: VISUAL DEBUGGING. Yes, I like visual debuggers. Really like them. For a few reasons, being the following two the main ones: I can see the values of the variables not only in the memory watches panels, but inlined into the code I can see the callstack But you may ask me: -What the devil build systems got to do with visual debugging?. And that is a pretty good question. While build systems do not prevent you from using visual debuggers, it limits the amount of information and insights you can get from the code. Some IDEs, for example, do not even allow for that. Why all of this matters? WAF. Waf is a pretty cool build system written entirely in Python and is supper easy to distribute with your project, so it needs only Python installed (which you would probably already have anyways) and whatever your project needs to get build (e.g. compiler). ns-3 have been using Waf for a while, and it has been working well for quite a long time, which goes to show it is not a bad build system in any way. My grudges with Waf is just that it does not support IDEs and the organization in different steps, plus how things are organized seem overly complicated for me. What I dislike the most however is that it does not produce intermediary build system files that I can inspect visually and discover what I am doing wrong. So I started working on CMake support... ns-3 with CMake Since I started my masters, my advisor said I should start working with ns-3. If it was not for that, I would probably be using ns-2 or OMNET++. Anyways, so I started rewriting everything ns-3 needed on CMake. Took quite some time to write everything, since I was not only translating Waf, but &quot;reverse engineering&quot;. I admit this was pretty stupid, but I simply could not understand how Waf did its magic (if it had written makefiles or something like that, it would have been easier and faster). Since 2018 I have been gathering feedback and refactoring everything to upstream that support, which seems like it is going to happen soon (release 3.36). With CMake, all major IDEs are supported from the get go: Microsoft Visual Studio and Visual Code, Jetbrains CLion, Apple XCode, Code::Blocks, Eclipse, CodeLite and others. Of course CMake can also be called directly via the command-line, but commands are very verbose. Command-line users will be happy to know they were not forgotten with the ns3 wrapper script for CMake (previously known as fakewaf and ns3waf). The script provides pretty much the same functionality expected from Waf, including exporting library paths, making programs just work instead of having to export the path first. My suggestion: use the ns3 wrapper when you need it. For example, when using IDEs that do not natively support CMake projects such as Xcode, Code::Blocks and Eclipse, you will need to specify the proper CMake Generator to get a project for them. Also, you will need to close the IDE, refresh the CMake cache manually (re-run ’ns3 configure’) whenever you add/remove a source file, create a new module or add a new dependency (either module or library), then you can open the IDE once again and continue using. I know it is pretty bad and that is why I suggest either CLion (my preferred IDE) or VsCode, which works surprisingly well with CMake. What changes for a module developer? Not that much. I have purposefully made it very similar to make the transition as smooth as possible. Copy and paste source lists to the appropriate variables, then clean semicolons and quotes and you are good to go if your module does not have any special needs (e.g. external library or options). If you need user-provided paths to look for libraries, you can follow example from the brite: # This part is similar the conf part in a wscript # I know it is pretty confusing, but this is due to CMake limitations # # First we declare/set a variable that points to a PATH in the CMake CACHE # (a.k.a. this value won&amp;#39;t be lost accross runs) # Then we declare/set a variable also stored in the cache, # but now it is used INTERNALly by us to determine if BRITE was # found in the NS3_WITH_BRITE path set(NS3_WITH_BRITE &amp;quot;&amp;quot; CACHE PATH &amp;quot;Build with brite support&amp;quot;) set(NS3_BRITE &amp;quot;OFF&amp;quot; CACHE INTERNAL &amp;quot;ON if Brite is found in NS3_WITH_BRITE&amp;quot;)</summary></entry><entry><title type="html">Blogging with LaTeX</title><link href="/2021/08/30/blogging-with-latex.html" rel="alternate" type="text/html" title="Blogging with LaTeX" /><published>2021-08-30T00:00:00+00:00</published><updated>2021-08-30T00:00:00+00:00</updated><id>/2021/08/30/blogging-with-latex</id><content type="html" xml:base="/2021/08/30/blogging-with-latex.html">&lt;h1 data-number=&quot;1&quot; id=&quot;introduction&quot;&gt;&lt;span class=&quot;header-section-number&quot;&gt;1&lt;/span&gt; Introduction&lt;/h1&gt;
&lt;p&gt;I have always said to myself that I should start blogging to keep track of things (projects, side-projects, side-projects of side-projects), but I have a terrible memory and end up forgetting that.&lt;/p&gt;
&lt;p&gt;This time around I was thinking: - Hey, I should start blogging about something.&lt;/p&gt;
&lt;p&gt;But you know what, I really suck at managing this kind of stuff, Jekyll helps quite a lot but I still need to write stuff with markdown.&lt;/p&gt;
&lt;p&gt;I am not a huge fan of markdown either, patterns are weird, formatting tables is a mess, no equations, no bibtex (I also miss that when working with Word), and the list goes on.&lt;/p&gt;
&lt;p&gt;Every engineer knows the best way to solve problems is to solve the problem for themselves by overengineering a solution (this is meant as a joke... even though it sounds too accurate to be just that...).&lt;/p&gt;
&lt;p&gt;So, how can we make things better for ourselves and get some decent syntax for writing somewhat large texts?&lt;/p&gt;
&lt;p&gt;Here is a tip: it is in the title of this post.&lt;/p&gt;
&lt;p&gt;Yes, we can translate LaTeX into HTML, do some stripping, add a new Jekyll header and make things work nicely.&lt;/p&gt;
&lt;p&gt;But how do we do that? Pandoc (Section &lt;a href=&quot;#Pandoc&quot; data-reference-type=&quot;ref&quot; data-reference=&quot;Pandoc&quot;&gt;2&lt;/a&gt;), Jekyll (Section &lt;a href=&quot;#Jekyll&quot; data-reference-type=&quot;ref&quot; data-reference=&quot;Jekyll&quot;&gt;3&lt;/a&gt;) and some script glue (Section &lt;a href=&quot;#glue&quot; data-reference-type=&quot;ref&quot; data-reference=&quot;glue&quot;&gt;4&lt;/a&gt;) written in the best programming language ever: Python.&lt;/p&gt;
&lt;h1 data-number=&quot;2&quot; id=&quot;Pandoc&quot;&gt;&lt;span class=&quot;header-section-number&quot;&gt;2&lt;/span&gt; Pandoc&lt;/h1&gt;
&lt;p&gt;Of course each one of us could write a LaTeX parser and do the conversion for ourselves (if we had unlimited time, big brains and were determined to do it).&lt;/p&gt;
&lt;p&gt;I do not know how about you, but I am of the kind that start writing programs right away just to realize how grand things will end up being.&lt;/p&gt;
&lt;p&gt;As usual when doing these things, you realize &quot;ain’t nobody got time for that&quot; &lt;span class=&quot;citation&quot; data-cites=&quot;AINT_GOT_TIME&quot;&gt;(NobodyGotTimeForThis, n.d.)&lt;/span&gt;.&lt;/p&gt;
&lt;p&gt;Started looking for alternatives. Found &lt;a href=&quot;https://github.com/latex2html/latex2html&quot;&gt;latex2html&lt;/a&gt; and &lt;a href=&quot;https://github.com/michal-h21/make4ht&quot;&gt;make4ht&lt;/a&gt;, which are pretty cool. Tried setting them up, but did not manage to make it work as I wanted. Started looking for more alternatives.&lt;/p&gt;
&lt;p&gt;Found a post in &lt;a href=&quot;https://www.danwjoyce.com/data-blog/2018/2/20/latex-to-html-via-pandoc&quot;&gt;Dr. Joyce’s blog&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;Pandoc: An universal markdown translator, which can magically translate a subset of LaTeX+BibTex into HTML+MathJax (yay /o/, equations). It is not perfect, but it also does not need to be.&lt;/p&gt;
&lt;p&gt;While I did not quite manage to make equation numbering nor manage citations style/cross-references working correctly, &lt;a href=&quot;https://github.com/tomduck/pandoc-xnos&quot;&gt;pandoc-xnos&lt;/a&gt; could make it work. I think it has to do with the glue in Section &lt;a href=&quot;#glue&quot; data-reference-type=&quot;ref&quot; data-reference=&quot;glue&quot;&gt;4&lt;/a&gt;. Maybe I will try again in the future, but for now it is doing what I needed.&lt;/p&gt;
&lt;p&gt;The magic command ended up being&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;    pandoc --number-sections --mathjax -f latex -t html -s --bibliography=file.bib -o file.html file.tex&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;p&gt;&lt;code&gt;--number-sections&lt;/code&gt; for numbered sections&lt;/p&gt;&lt;/li&gt;
&lt;li&gt;&lt;p&gt;&lt;code&gt;--mathjax&lt;/code&gt; to process equations and render using javascript&lt;/p&gt;&lt;/li&gt;
&lt;li&gt;&lt;p&gt;&lt;code&gt;-f latex&lt;/code&gt; to process from LaTeX&lt;/p&gt;&lt;/li&gt;
&lt;li&gt;&lt;p&gt;&lt;code&gt;-t html&lt;/code&gt; to output in html&lt;/p&gt;&lt;/li&gt;
&lt;li&gt;&lt;p&gt;&lt;code&gt;-s&lt;/code&gt; standalone html&lt;/p&gt;&lt;/li&gt;
&lt;li&gt;&lt;p&gt;&lt;code&gt;--bibliography=file.bib&lt;/code&gt; to get references from the bib file&lt;/p&gt;&lt;/li&gt;
&lt;li&gt;&lt;p&gt;&lt;code&gt;-o file.html&lt;/code&gt; to indicate the path to the output file&lt;/p&gt;&lt;/li&gt;
&lt;li&gt;&lt;p&gt;&lt;code&gt;file.tex&lt;/code&gt; to indicate the input file&lt;/p&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h1 data-number=&quot;3&quot; id=&quot;Jekyll&quot;&gt;&lt;span class=&quot;header-section-number&quot;&gt;3&lt;/span&gt; Jekyll&lt;/h1&gt;
&lt;p&gt;You probably have heard of &lt;a href=&quot;https://jekyllrb.com/&quot;&gt;Jekyll&lt;/a&gt; at this point. It is a nice static site generator used by &lt;a href=&quot;https://pages.github.com/&quot;&gt;GitHub Pages&lt;/a&gt;, which made a ton of people used to it by default. Jekyll does not support LaTeX documents, but it does accept HTML files as inputs.&lt;/p&gt;
&lt;p&gt;By placing the post header in the HTMLs, it will treat the HTML contents as the post content.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;---
layout: post # could be a different layout
author: &amp;quot;authorname&amp;quot;
title: &amp;quot;postname&amp;quot;
---&lt;/code&gt;&lt;/pre&gt;
&lt;h1 data-number=&quot;4&quot; id=&quot;glue&quot;&gt;&lt;span class=&quot;header-section-number&quot;&gt;4&lt;/span&gt; Script glue&lt;/h1&gt;
&lt;p&gt;The final piece is the code glue written in Python. It just scans for &lt;em&gt;.tex&lt;/em&gt; files, uses pandoc to convert them, strips out unnecessary HTML header/footer and include the post header expected by Jekyll containing the post author names, title and layout.&lt;/p&gt;
&lt;p&gt;If there is a &lt;em&gt;.bib&lt;/em&gt; file along with the &lt;em&gt;.tex&lt;/em&gt;, it is used as the bibliography source file.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;def latex_to_html_via_pandoc(source_file, source_dir=&amp;quot;latex_posts&amp;quot;, target_dir=&amp;quot;_posts&amp;quot;):

    output_file = source_file.replace(&amp;quot;.tex&amp;quot;, &amp;quot;.html&amp;quot;).replace(source_dir, target_dir)

    # Latex to html
    command = &amp;quot;&amp;quot;&amp;quot;pandoc --number-sections --mathjax -f latex -t html -s&amp;quot;&amp;quot;&amp;quot;
    command = command.split(&amp;quot; &amp;quot;)

    # Load references from bib file if it exists
    bibliography = source_file.replace(&amp;quot;.tex&amp;quot;, &amp;quot;.bib&amp;quot;)
    if os.path.exists(bibliography):
        command.append(&amp;quot;--bibliography=%s&amp;quot; % bibliography)  # use external bib file or not

    command.append(&amp;quot;-o&amp;quot;)
    command.append(output_file)  # output path
    command.append(source_file)  # source path

    # Run pandoc
    try:
        subprocess.check_output(command)
    except Exception as e:
        raise Exception(&amp;quot;Error during pandoc conversion of %s: %s&amp;quot; % (source_file, e))

    # Open the html file and get only contents to let jekyll handle style, links, etc
    with open(output_file, &amp;quot;r&amp;quot;, encoding=&amp;quot;utf-8&amp;quot;) as f:
        contents = f.read()

        # extract authors and title
        authors = re_match_html_author.findall(contents)
        author = authors[0]
        authors.pop(0)
        while len(authors) &amp;gt; 0:
            author += &amp;quot;, &amp;quot; + authors[0]
            authors.pop(0)
        del authors
        title = re_match_html_title.findall(contents)[0]

        # Remove unnecessary header and trailer
        contents = contents.split(&amp;quot;&amp;lt;/header&amp;gt;&amp;quot;)[1]
        contents = contents.split(&amp;quot;&amp;lt;/body&amp;gt;&amp;quot;)[0]

    # Rewrite file with markdown header
    with open(output_file, &amp;quot;w&amp;quot;, encoding=&amp;quot;utf-8&amp;quot;) as f:
        # Write markdown header
        f.write(&amp;quot;&amp;quot;&amp;quot;---\nlayout: post\nauthor: &amp;quot;%s&amp;quot;\ntitle: &amp;quot;%s&amp;quot;\n---&amp;quot;&amp;quot;&amp;quot; % (author, title))

        # Write stripped down html
        f.write(contents)&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;After that, you can manually run jekyll build command or jekyll serve (which starts the webserver).&lt;/p&gt;
&lt;p&gt;Or run &lt;code&gt;Latex2Markdown.py --serve True&lt;/code&gt; to run the LaTeX to HTML, Jekyll build and start service at once.&lt;/p&gt;
&lt;p&gt;Sources for this blog are available &lt;a href=&quot;https://gabrielcarvfer.github.io/Latex2Markdown.py&quot;&gt;here&lt;/a&gt;.&lt;/p&gt;
&lt;h1 data-number=&quot;5&quot; id=&quot;conclusions&quot;&gt;&lt;span class=&quot;header-section-number&quot;&gt;5&lt;/span&gt; Conclusions&lt;/h1&gt;
&lt;p&gt;It works surprisingly well and I am pretty happy with the results.&lt;/p&gt;
&lt;h1 class=&quot;unnumbered&quot; data-number=&quot;&quot; id=&quot;references&quot;&gt;References&lt;/h1&gt;
&lt;div id=&quot;refs&quot; class=&quot;references hanging-indent&quot; role=&quot;doc-bibliography&quot;&gt;
&lt;div id=&quot;ref-AINT_GOT_TIME&quot;&gt;
&lt;p&gt;NobodyGotTimeForThis. n.d. “Ain’t Nobody Got Time for That (Original + Autotune).” &lt;a href=&quot;https://youtu.be/waEC-8GFTP4&quot;&gt;https://youtu.be/waEC-8GFTP4&lt;/a&gt;.&lt;/p&gt;
&lt;/div&gt;
&lt;/div&gt;</content><author><name>Gabriel</name></author><summary type="html">1 Introduction I have always said to myself that I should start blogging to keep track of things (projects, side-projects, side-projects of side-projects), but I have a terrible memory and end up forgetting that. This time around I was thinking: - Hey, I should start blogging about something. But you know what, I really suck at managing this kind of stuff, Jekyll helps quite a lot but I still need to write stuff with markdown. I am not a huge fan of markdown either, patterns are weird, formatting tables is a mess, no equations, no bibtex (I also miss that when working with Word), and the list goes on. Every engineer knows the best way to solve problems is to solve the problem for themselves by overengineering a solution (this is meant as a joke... even though it sounds too accurate to be just that...). So, how can we make things better for ourselves and get some decent syntax for writing somewhat large texts? Here is a tip: it is in the title of this post. Yes, we can translate LaTeX into HTML, do some stripping, add a new Jekyll header and make things work nicely. But how do we do that? Pandoc (Section 2), Jekyll (Section 3) and some script glue (Section 4) written in the best programming language ever: Python. 2 Pandoc Of course each one of us could write a LaTeX parser and do the conversion for ourselves (if we had unlimited time, big brains and were determined to do it). I do not know how about you, but I am of the kind that start writing programs right away just to realize how grand things will end up being. As usual when doing these things, you realize &quot;ain’t nobody got time for that&quot; (NobodyGotTimeForThis, n.d.). Started looking for alternatives. Found latex2html and make4ht, which are pretty cool. Tried setting them up, but did not manage to make it work as I wanted. Started looking for more alternatives. Found a post in Dr. Joyce’s blog. Pandoc: An universal markdown translator, which can magically translate a subset of LaTeX+BibTex into HTML+MathJax (yay /o/, equations). It is not perfect, but it also does not need to be. While I did not quite manage to make equation numbering nor manage citations style/cross-references working correctly, pandoc-xnos could make it work. I think it has to do with the glue in Section 4. Maybe I will try again in the future, but for now it is doing what I needed. The magic command ended up being pandoc --number-sections --mathjax -f latex -t html -s --bibliography=file.bib -o file.html file.tex --number-sections for numbered sections --mathjax to process equations and render using javascript -f latex to process from LaTeX -t html to output in html -s standalone html --bibliography=file.bib to get references from the bib file -o file.html to indicate the path to the output file file.tex to indicate the input file 3 Jekyll You probably have heard of Jekyll at this point. It is a nice static site generator used by GitHub Pages, which made a ton of people used to it by default. Jekyll does not support LaTeX documents, but it does accept HTML files as inputs. By placing the post header in the HTMLs, it will treat the HTML contents as the post content. --- layout: post # could be a different layout author: &amp;quot;authorname&amp;quot; title: &amp;quot;postname&amp;quot; --- 4 Script glue The final piece is the code glue written in Python. It just scans for .tex files, uses pandoc to convert them, strips out unnecessary HTML header/footer and include the post header expected by Jekyll containing the post author names, title and layout. If there is a .bib file along with the .tex, it is used as the bibliography source file. def latex_to_html_via_pandoc(source_file, source_dir=&amp;quot;latex_posts&amp;quot;, target_dir=&amp;quot;_posts&amp;quot;):</summary></entry><entry><title type="html">Warp-speeding the captain of the Starfleet</title><link href="/metabrainz/2020/12/17/picard-the-captain-of-the-starfleet.html" rel="alternate" type="text/html" title="Warp-speeding the captain of the Starfleet" /><published>2020-12-17T00:00:00+00:00</published><updated>2020-12-17T00:00:00+00:00</updated><id>/metabrainz/2020/12/17/picard-the-captain-of-the-starfleet</id><content type="html" xml:base="/metabrainz/2020/12/17/picard-the-captain-of-the-starfleet.html">&lt;p&gt;Have you ever heard of &lt;a href=&quot;https://picard.musicbrainz.org/&quot;&gt;MusicBrainz Picard&lt;/a&gt;? 
It is an amazing piece of software that makes tagging music files a breeze. 
Not only that, but it can also be used to fingerprint your music files, identify the recording and pull up metadata
from the MusicBrainz servers.&lt;/p&gt;

&lt;p&gt;I got some spare time earlier this year and decided to tag my long-abandoned music library. Started by downloading the
latest version of Picard, then installed it. After that I executed the binary and finally clicked to scan my library folder.&lt;/p&gt;

&lt;p&gt;And then I waited… And waited… And waited… Jesus, what is this thing doing? It was barely touching the disk drive, but
the single thread was pinned at 100% load. It clearly had a bottleneck somewhere and I had to find it. Started looking into 
the code that performed the load and the profilers quickly indicated troublesome spots, which I thought could be spread into
multiple processes to speed things up. Spent some time trying to make it work and failed miserably due to circular dependencies
between the logic components and the GUI.&lt;/p&gt;

&lt;p&gt;Then I tried looking in the forums because I must be doing something wrong. I have found a few people complaining about performance.
After that, I dug their issue tracker and hit gold. &lt;a href=&quot;https://tickets.metabrainz.org/browse/PICARD-975&quot;&gt;Issue 975 - Reimplement the threading code in Picard&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;One of Picard’s contributors, Sophist gave me a few hints &lt;a href=&quot;https://tickets.metabrainz.org/browse/PICARD-975?focusedCommentId=53339&amp;amp;page=com.atlassian.jira.plugin.system.issuetabpanels%3Acomment-tabpanel#comment-53339&quot;&gt;where to look at&lt;/a&gt;.
After assuming the bottleneck was CPU-bound, I’ve proposed a &lt;a href=&quot;https://tickets.metabrainz.org/browse/PICARD-975?focusedCommentId=53340&amp;amp;page=com.atlassian.jira.plugin.system.issuetabpanels%3Acomment-tabpanel#comment-53340&quot;&gt;multi-process architecture&lt;/a&gt;. It ended up being a dumb 
way to deal with the issue, but we will come to that later.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/assets/img/2020-12-17-picard-the-captain-of-the-starfleet/picard_multiprocess.png&quot; alt=&quot;drawing&quot; width=&quot;100%&quot; /&gt;&lt;/p&gt;

&lt;p&gt;We then discussed a bit and I started looking into the areas suggested by Sophist. I decided that batch processing would probably 
speed things up, as instructions/data would be cached. I was partially correct, and managed to reduce load times from 
&lt;a href=&quot;https://tickets.metabrainz.org/browse/PICARD-975?focusedCommentId=53349&amp;amp;page=com.atlassian.jira.plugin.system.issuetabpanels%3Acomment-tabpanel#comment-53349&quot;&gt;20 minutes down to 8 minutes&lt;/a&gt;.
It was huge. Got pretty excited with the results and decided to properly profile things.&lt;/p&gt;

&lt;p&gt;And then we hit the first major breakthrough:&lt;/p&gt;

&lt;h4 id=&quot;1---when-the-slow-path-is-too-slow-having-a-fast-path-is-a-good-idea&quot;&gt;1 - When the slow path is too slow, having a fast path is a good idea&lt;/h4&gt;

&lt;p&gt;The cProfiler indicated that most of the remaining time during load was due to trying to guess the file format based on
file headers instead of using the file extension. It so happens that most of the time the files extensions are correct,
and we could skip trying to identify the file. If the load failed, we could fallback to trying to guess the file format.
That alone reduced load times even further, from 20 minutes down to 3.5 minutes.
It became my first &lt;a href=&quot;https://github.com/metabrainz/picard/pull/1529&quot;&gt;pull request&lt;/a&gt; for Picard. Pretty exciting.&lt;/p&gt;

&lt;hr /&gt;

&lt;p&gt;After that, I tried a bunch more stuff to make things faster, but nothing seemed to change.
Then I started looking at the IO performance. Mainstream Picard barely reached 3MBps while reading the files.
The batch version was much faster, reaching up to &lt;a href=&quot;https://tickets.metabrainz.org/browse/PICARD-975?focusedCommentId=53354&amp;amp;page=com.atlassian.jira.plugin.system.issuetabpanels%3Acomment-tabpanel#comment-53354&quot;&gt;39MBps&lt;/a&gt;, 
but it also prevented the UI from updating, causing freezes.&lt;/p&gt;

&lt;p&gt;There was definitely something messed up, and thread starvation seemed to make sense. It so happens that the directory
scanning thread, that fed file loading threads, was executed concurrently instead of running at once and then dispatching
the work. That made Picard randomly access the disk while trying to load songs at the same time it scanned directories.
A.k.a. the worst-case scenario for HDDs.&lt;/p&gt;

&lt;p&gt;By scanning first and processing later, we drastically reduced thread wait times.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/assets/img/2020-12-17-picard-the-captain-of-the-starfleet/picard_threading_profiling.png&quot; alt=&quot;drawing&quot; width=&quot;100%&quot; /&gt;&lt;/p&gt;

&lt;p&gt;And then we hit the second major breakthrough:&lt;/p&gt;

&lt;h4 id=&quot;2---poor-loading-threads-were-starving-to-death-while-scanning-thread-was-trying-to-keep-up&quot;&gt;2 - Poor loading threads were starving to death while scanning thread was trying to keep up&lt;/h4&gt;

&lt;p&gt;Improving the scanning thread and letting it completely scan the directories before dispatching work for other threads 
was essential to boost performance. 
This change alone reduced loading times of a different library from 32 minutes down to 13 minutes, as reported in 
my second &lt;a href=&quot;https://github.com/metabrainz/picard/pull/1531&quot;&gt;pull request&lt;/a&gt;.&lt;/p&gt;

&lt;hr /&gt;

&lt;p&gt;We ended up moving discussion from their issue tracker to that previous pull request thread.
The IO issues seemed to be solved, but now it was the UI that could not keep up with the file loading, causing freezes
and stutters. This is where things turned out way more difficult to profile than I initially expected.&lt;/p&gt;

&lt;p&gt;Tried some really complicated stuff, like &lt;a href=&quot;https://github.com/metabrainz/picard/pull/1531#issuecomment-625004549&quot;&gt;caching metadata to skip reloading files&lt;/a&gt; 
and part of the &lt;a href=&quot;https://github.com/metabrainz/picard/pull/1531#issuecomment-626184738&quot;&gt;multi-process architecture&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;Profilers pointed at different things all the time: first &lt;a href=&quot;https://github.com/metabrainz/picard/pull/1531#issuecomment-623181143&quot;&gt;updating the file items&lt;/a&gt;, 
then &lt;a href=&quot;https://github.com/metabrainz/picard/pull/1531#issuecomment-626377501&quot;&gt;the file loading finalization&lt;/a&gt;, 
then &lt;a href=&quot;https://github.com/metabrainz/picard/pull/1531#issuecomment-629534654&quot;&gt;the sorting algorithm&lt;/a&gt;,
and finally &lt;a href=&quot;https://github.com/metabrainz/picard/pull/1531#issuecomment-629534654&quot;&gt;the sorting of the panels&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;Surprisingly, temporarily disabling the sorting of the panels was super effective, and our third breakthrough.&lt;/p&gt;

&lt;h4 id=&quot;3---sorting-over-and-over-an-ever-increasing-list-is-insane-and-ui-draws-are-super-expensive&quot;&gt;3 - Sorting over and over an ever-increasing list is insane and UI draws are super expensive&lt;/h4&gt;

&lt;p&gt;We disabled the sorting during operations that involve moving/adding/removing large amounts of files (e.g. loading/clustering).
This reduced drawing times of the items in the panel from &lt;a href=&quot;https://github.com/metabrainz/picard/pull/1531#issuecomment-629534654&quot;&gt;30 minutes down to 1 minute&lt;/a&gt;.&lt;/p&gt;

&lt;hr /&gt;

&lt;p&gt;It was at this point I realized the previous changes were way too much and we could achieve basically the same great results with fewer changes.
Remember when I said the multi-process solution was dumb? This is the point I realized it was the case.&lt;/p&gt;

&lt;p&gt;Putting the previous 2 breakthroughs into a &lt;a href=&quot;https://github.com/metabrainz/picard/pull/1543&quot;&gt;single PR&lt;/a&gt;, 
we manage to reduce loading times of a third library from &lt;strong&gt;50m down to 6m&lt;/strong&gt;.
At the same time, clustering times fell from &lt;strong&gt;5h17min down to 3 minutes&lt;/strong&gt;.&lt;/p&gt;

&lt;p&gt;But I was not done just yet. I could feel the profilers were messing with me. How could I have changed a ton of stuff,
and instead of processing times falling they would just move around to different functions? Quite puzzling.&lt;/p&gt;

&lt;p&gt;The answer was the profilers just could not measure the problem because it was not in python, but outside python.
What do I mean by that? The C libraries that took the GIL and blocked python were the culprits. I decided to 
move some of the modules to cython to track exactly where the calls were made and voilà. Two missing Qt size hints were
&lt;a href=&quot;https://github.com/metabrainz/picard/pull/1555#issuecomment-644475419&quot;&gt;two large contributors to the UI slowdown&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;Our fourth breakthrough:&lt;/p&gt;

&lt;h4 id=&quot;4---qt-documentation-is-poor-and-profiling-mixed-language-software-is-nightmarish&quot;&gt;4 - Qt documentation is poor, and profiling mixed language software is nightmarish&lt;/h4&gt;

&lt;p&gt;Yes, two Qt size hints were enough to improve the UI responsiveness by a huge margin. Those were two one-liners
but reduced the CPU samples from 40% down to 10% of total samples. This means Picard can actually do useful stuff
instead of guessing sizes during repaints of the UI.&lt;/p&gt;

&lt;p&gt;&lt;em&gt;Without hints&lt;/em&gt; = 40% of CPU samples
&lt;img src=&quot;/assets/img/2020-12-17-picard-the-captain-of-the-starfleet/picard_without_hint.png&quot; alt=&quot;drawing&quot; width=&quot;100%&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;em&gt;With hints&lt;/em&gt; = 10% of CPU samples
&lt;img src=&quot;/assets/img/2020-12-17-picard-the-captain-of-the-starfleet/picard_with_hint.png&quot; alt=&quot;drawing&quot; width=&quot;100%&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Side effects? &lt;a href=&quot;https://github.com/metabrainz/picard/pull/1555#issuecomment-644860437&quot;&gt;150x speedup&lt;/a&gt;
on moving files during clustering with expanded clusters (worst case scenario) + sorting disabled. 
People used to recommend collapsing the clusters to speed things up, but it got solved by two one-line hints.
Completely crazy.&lt;/p&gt;

&lt;hr /&gt;

&lt;p&gt;With this, I was pretty much done, but the UI still had some freezing/hiccup issues, especially in Windows. 
After further profiling, I have noticed the worker threads had a lot of contention while checking configuration variables.
They spent a ton of time waiting for each other due to locks. To prevent them from locking, we only locked 
the reader threads if the cached setting was marked as stale/dirty or when a thread needed the lock to update the setting.
This was done in this &lt;a href=&quot;https://github.com/metabrainz/picard/commit/18c4e93cf1073e21857e4e39d311eb28d9d8f76f&quot;&gt;commit&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;Other area of Picard that drastically slowed things down was selecting tracks. They updated the metadata box, which can be 
awfully expensive depending on how many items are selected. I tried to mitigate the issue &lt;a href=&quot;https://github.com/metabrainz/picard/commit/6ef6679f65e68796cfdf8be2d63307f5b1400305&quot;&gt;here&lt;/a&gt;,
by preventing metadata box updates if the selection didn’t change. It seemed very logical to me (as I am trying to prevent reprocessing everything),
but it ended up causing issues to other users. Tried a few more changes later, but it still ended up being changed as users reported other issues… My bad.&lt;/p&gt;

&lt;h3 id=&quot;end-results&quot;&gt;End results&lt;/h3&gt;

&lt;p&gt;As reported in ticket &lt;a href=&quot;https://tickets.metabrainz.org/browse/PICARD-1844&quot;&gt;PICARD-1844 - Further improve loading and clustering performance&lt;/a&gt;,
loading times fell from 50m down to 5m30s (~9x speedup), and clustering times fell from 5h to 2m (~150x speedup) in my library with 19k files. 
UI responsiveness is way harder to measure, so no hard measures on that, but you can try it by yourself. 
Touching mere 700 lines of code instead of rearchitecting the entire thing as I initially thought would be necessary…
Changes were released in Picard 2.4.&lt;/p&gt;

&lt;p&gt;It was pretty fun and the results were great. 
Laurent (Zas), Philipp (outsidecontext) and Sophist (I don’t know his real name) were awesome and helped a lot.&lt;/p&gt;</content><author><name></name></author><category term="MetaBrainz" /><summary type="html">Have you ever heard of MusicBrainz Picard? It is an amazing piece of software that makes tagging music files a breeze. Not only that, but it can also be used to fingerprint your music files, identify the recording and pull up metadata from the MusicBrainz servers.</summary></entry></feed>